import Layout from '~/components/Layout';
import Button from '~/components/Button';

export const Pagina = () => {
  return (
    <Layout>
      <Button nome='Teste'></Button>
    </Layout>
  );
};

export default Pagina;
